# Database
# DATABASE_URL="postgresql://seed:root@localhost:5432/attendease?schema=public"
DATABASE_URL = "postgresql://neondb_owner:npg_4YQdF5ExJepD@ep-lucky-frog-a1c1bbq1.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRATION="7d"

# Server
PORT=3000
NODE_ENV="development"

# Biometric API (for future integration)
BIOMETRIC_API_URL=""
BIOMETRIC_API_KEY=""

# Optional: SMTP for password reset emails
# If not set, reset links are logged to the server console (for local dev)
# Gmail: use App Password from Google Account > Security > 2-Step Verification > App passwords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=your-email@gmail.com

# Frontend URL (used for CORS and password reset links)
# FRONTEND_URL="http://localhost:5173"