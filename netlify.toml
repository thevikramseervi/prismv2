[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serve index.html for every route so React Router works.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
